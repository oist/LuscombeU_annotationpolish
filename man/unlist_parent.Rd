% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unlist_parent.R
\name{unlist_parent}
\alias{unlist_parent}
\title{Extract First Parent from GRanges Object Metadata}
\usage{
unlist_parent(gr)
}
\arguments{
\item{gr}{}
}
\value{
A modified \code{GRanges} object with the \code{Parent} metadata column simplified to contain only the first element of any list present. If the original \code{Parent} column contains \code{NULL} or an empty list for a particular range, that entry is replaced with \code{NA}.
}
\description{
This function takes a \code{GRanges} object and processes the \code{Parent} metadata column. For each entry in the \code{Parent} column, it extracts the first element if the column contains a list. This is useful in scenarios where the \code{Parent} metadata is stored as lists of identifiers, and you are interested in simplifying the structure by retaining only the first parent identifier for each range.
}
\examples{
# Assuming `gr` is a GRanges object with a complex 'Parent' metadata column
# Simplify the 'Parent' column to only keep the first parent identifier
simplified_gr <- unlist_parent(gr)

# Example GRanges object creation (for users unfamiliar with GRanges):
# gr <- GRanges(seqnames = "chr1",
#               ranges = IRanges(start = c(100, 200), end = c(150, 250)),
#               strand = c("+", "-"),
#               Parent = list(c("gene1", "gene2"), "gene3"))
# This will modify the 'Parent' column of `gr` to have "gene1" for the first range
# and "gene3" for the second range.
}
